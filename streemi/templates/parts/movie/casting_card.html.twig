<div class="flex flex-col items-center text-center p-4">
    <img src="{{ cast.avatarUrl }}" alt="{{ cast.name }}" class="h-20 w-20 object-cover rounded-full mb-2 border">
    <span class="text-sm font-medium">{{ cast.name }}</span>
    <span class="text-xs text-gray-500">{{ cast.role }}</span>
</div>
