<li class="flex items-center mb-3 space-x-3">
    <a href="{{ path('show_movie', {id: movie.id}) }}">
        <img src="{{ movie.coverImage }}" alt="{{ movie.title }}" class="h-16 w-12 rounded-lg object-cover">
    </a>
    <div>
        <a href="{{ path('show_movie', {id: movie.id}) }}" class="text-sm font-semibold hover:text-red-600">{{ movie.title }}</a>
        <p class="text-xs text-gray-500">{{ movie.remainingTime }} restants</p>
    </div>
</li>
