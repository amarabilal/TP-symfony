<li class="relative flex flex-col rounded-xl overflow-hidden border hover:scale-105 transition cursor-pointer">
    <a href="{{ path('show_movie', {id: playlistMedia.media.id}) }}" class="absolute top-2 right-2 p-2 bg-gray-800 rounded-lg hover:bg-red-600">
        <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clip-rule="evenodd" />
        </svg>
    </a>
    <img src="{{ playlistMedia.media.coverImage }}" alt="{{ playlistMedia.media.title }}" class="h-80 object-cover">
    <div class="bg-white p-3 flex justify-between border-t-2 border-red-600">
        <span class="font-medium">{{ playlistMedia.media.title }}</span>
        <span class="text-xs">2001</span>
    </div>
</li>
